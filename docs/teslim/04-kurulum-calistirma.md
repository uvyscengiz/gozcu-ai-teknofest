# ④ Projenin çalıştırılması için adım adım talimatlar

**Gözcü** · Takım **FERASET** (`team37`) · Muğla Sıtkı Koçman Üniversitesi

Bu bölüm şartname §6'nın *"projenin çalıştırılması için adım adım talimatlar
(kurulum, çalıştırma)"* kalemidir.

> **TODO — README.md'den ve koddan yazılacak.** Kaynaklar: `README.md`
> (kurulum adımları), `pyproject.toml` (bağımlılıklar), `.env.example`
> (çevre değişkenleri), `app.py` (giriş noktası).

## Kapatılması gereken başlıklar

- Ön koşullar: Python sürümü, uv, sistem paketleri (ffmpeg vb.)
- Kurulum: `uv sync`, `.env` hazırlığı
- Çalıştırma: `uv run python app.py` — video yükleme → çıktı akışı
- Çevre değişkenleri: her birinin ne işe yaradığı
- Donanım gereksinimleri: minimum / önerilen
